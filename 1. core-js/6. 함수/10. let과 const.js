
let x;

console.log(x);

const y ='hello';
// y=100;
console.log(y);

const COLOR_YELLOW = `#ff0`;
const COLOR_GREEN = `#0f0`;
const COLOR_BLACK = `#000`;
// $div.style.background = COLOR_YELLOW;
// $div.style.background = COLOR_BLACK;
// $div.style.background = COLOR_GREEN;


let kim = {
    name: `김철수`,
    age: 30,
    
};

kim.name = `김마이클`

// kim = {
//     name: `박영희`,
//     age: 40,
// }

console.log(kim);

// 자바스크립트에서는 모든것이 const(상수)다.
// 값이 변경되는 제한적인 변수에만 let으로 변경해라.

const a = 10;
const b = 20;